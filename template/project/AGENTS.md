# {{PROJECT}} — agent contract

Read this before making changes. Keep it under one screen.

## What this project is

<!-- One paragraph: domain, runtime, primary users. Replace this. -->

## Stack

<!-- Languages, frameworks, package manager, test runner, deploy target. -->

## How to run it

```bash
# install
# dev
# test
# build
```

## Conventions

- Code style: see `.editorconfig` and any project-level lint config.
- Commits: <!-- conventional commits? squash? trunk-based? -->
- Branching: <!-- feature/*, main, etc. -->

## Don't

<!-- Project-specific traps: files to never touch, patterns to avoid. -->

## Secrets

- Stored in macOS Keychain via the `kc` shell helper (e.g. `$(kc {{PROJECT}}_API_KEY)`).
- `.env.example` is the source of truth for required keys.
